# RAG 结课项目展示问答稿

这个文件用于把答辩时容易被问到的问题、当前系统已经完成的能力、仍需补强的地方，以及下一步可演示改进集中放在一起。汇报时可以按这里的顺序讲，不需要在代码、README 和前端页面之间来回找材料。

## 1. 展示核心问题

### Q1：如何结合课程问答数据、研究论文数据和项目要求做检索前/检索后优化？

我们的系统把任务拆成两条链路：

1. **课程 QA 链路**：课程 QA JSON 是任务输入，包含一个问题和 10 个候选回答。系统不把这些候选回答当作知识库证据，而是在 07 响应生成阶段读取题目和候选答案，再从外部知识库 collection 检索相关证据，最后让大模型结合证据对候选回答排序。
2. **论文 RAG 链路**：论文材料作为知识库进入普通 RAG 流程，用户提出论文相关问题后，系统检索论文 chunk 并生成带证据的回答。

检索前优化主要包括：

- **数据入口分流**：课程 QA JSON、课程外部知识文档、论文文档分别导入，避免把任务标签、候选答案和外部知识混在一个索引库里。
- **文件解析细化**：本地 PDF 解析支持 PyMuPDF、PyPDF、Unstructured、PDF Plumber；01 文档导入和 03 文件解析都支持 MinerU VLM 精准解析。03 用于预览对比，01 会把 MinerU 结果写入本地导入文档，进入后续 RAG。
- **分块策略区分**：课程 QA 一题一块，课程知识文档和 Markdown 资料按标题自然分块，论文材料保留章节、页码和表格信息。
- **嵌入模型选择**：04 保留 Qwen 百炼 embedding 和 HuggingFace embedding。HF 保留中文轻量模型 `BAAI/bge-small-zh-v1.5`，同时提供轻量多语言模型 `intfloat/multilingual-e5-small`。
- **索引方式对比**：05 支持 Chroma HNSW 和 FAISS Flat / IVF / LSH，用来比较不同向量索引策略的召回效果、速度和可解释性。

检索后优化主要包括：

- **TopK 证据约束**：07 默认 Top K 为 3，避免把太多低相关证据塞进 prompt。
- **证据分数展示**：已引用证据显示相似性分数，`SearchHit.score` 保持越大越相关。
- **任务化 prompt**：课程 QA prompt 明确要求对候选答案排序；论文 RAG prompt 维持普通基于证据的问答。
- **LLM-only 防混淆**：LLM-only 模式不渲染 RAG 证据，避免模型把自己的判断误写成“已引用证据”。
- **向量可视化辅助解释**：04 和 07 都提供后端降维后的向量视图；07 会特别标出 Query 点和 TopK 命中点。

### Q2：如何在系统中提高响应生成质量？

回答质量主要从三层提高：

1. **输入质量**：结构化导入课程 QA，隐藏 `answer_quality` 标签；对外部知识文档做标题分块；论文 digest 保留章节和表格信息。
2. **检索质量**：使用合适的 embedding、索引方式和 TopK；通过分数和向量视图检查命中的证据是否真正相关。
3. **生成质量**：课程 QA 和论文 RAG 使用不同任务定义；RAG 模式只允许引用真实检索证据；LLM-only 不展示证据，避免把模型幻觉当作检索结果。

## 2. 项目要求完成情况

| 老师要求 | 当前完成情况 | 还需补强 |
| --- | --- | --- |
| 课程提供程序在数据上调通运行 | 已有课程 QA JSON 导入、题目选择、候选答案排序、外部知识 RAG 支持 | 需要补一组固定测试样例的结果表，展示 LLM-only 和 RAG 的差异 |
| 数据包括课程小组数据和研究论文 | 已支持课程 QA 和论文 RAG 两条任务链路 | 需要最终确认展示用论文 PDF/Markdown 来源 |
| 多种嵌入模型和生成模型 | 04 支持 Qwen 百炼 embedding 与 HF embedding；07 使用百炼生成 | 如需更完整对比，可补一个生成模型配置说明 |
| 前端 React 增加功能 | 已增加向量可视化视图、课程 QA 模式、索引 provider 选择等 | 可以把评测结果做成更清晰的 dashboard |
| 文件解析功能细化 | 已有多种本地解析方法；01/03 均支持 MinerU VLM 精准解析 | 可继续补解析方式对比表，展示复杂版式、表格和公式的差异 |
| 向量库索引对比分析 | 已支持 Chroma HNSW、FAISS Flat、FAISS IVF、FAISS LSH | 需要补一页实验对比：召回样例、耗时、优缺点 |
| 响应生成增加检索前/检索后优化 | 已区分课程 QA 与论文 RAG；加入 TopK、证据分数、LLM-only 证据隔离 | 需要把优化前后结果量化，形成可汇报表格 |

## 3. 推荐演示路径

### 课程 QA 任务

1. 在 01 文档导入中上传课程 QA JSON，作为任务输入。
2. 在 01 文档导入中上传课程外部知识文档，例如 `sample_data/daily_life_knowledge_reference.md`。
3. 在 02 分块中对外部知识文档按标题/自然结构分块。
4. 在 04 向量存储中选择 Qwen 或 HF embedding 生成向量。
5. 在 05 向量库索引中选择 Chroma 或 FAISS，并创建 collection。
6. 在 07 响应生成中切到课程 QA，选择题目和外部知识 collection，运行 RAG 排序。
7. 展示证据分数、Query 点、TopK 命中点和最终候选答案排序。

### 论文 RAG 任务

1. 导入论文文档或使用已有论文 digest。
2. 分块、embedding、索引。
3. 在 07 响应生成中切到论文 RAG。
4. 输入论文问题，展示检索证据和最终回答。

### 文件解析能力展示

03 文件解析是一个独立的解析预览页，不是主 RAG 链路必经步骤。可以用于展示：

- 本地解析方法之间的差异。
- MinerU VLM 精准解析对复杂版式、图片、表格、公式文档的解析能力。
- 为什么解析质量会影响后续 chunk、embedding 和检索质量。

如果要让 MinerU 解析结果真正进入 RAG，请在 01 文档导入中选择 `文档 / PDF -> MinerU VLM 精准解析`。系统会把 MinerU 的 `full.md` 转为本地 `01-loaded-docs/*.json`，再继续 02 分块、04 向量存储、05 索引和 07 响应生成。

## 4. MinerU API 改进说明

当前接入的是 **MinerU VLM 精准解析 API**：

- 前端 01 和 03 都可选择 `MinerU VLM 精准解析` 后上传文件。
- 后端读取 `MINERU_API_TOKEN`，调用 `https://mineru.net/api/v4/file-urls/batch` 创建签名上传任务。
- 上传本地文件后，后端通过 `extract-results/batch/{batch_id}` 轮询解析状态。
- MinerU 完成后返回 `full_zip_url`，后端下载 zip 并读取其中的 `full.md`。03 会用于页面预览；01 会把 Markdown 章节持久化为导入文档。
- 这是一个真实外部 API，不是 mock，也没有本地兜底；失败时直接返回错误。

适合汇报时这样解释：

> 我们把文件解析从单纯本地 PDF text extraction 扩展到 MinerU VLM 精准解析 API。这样可以更好处理复杂版式、表格、公式和图片，对后续 RAG 的 chunk 质量有直接帮助。为了避免密钥泄露，token 只通过 `.env` 或环境变量提供，不进入源码、README 示例值、日志或前端。

需要注意：

- VLM 精准解析需要 `MINERU_API_TOKEN`，并受官方文件大小、页数和额度限制。
- 当前默认地址是 `https://mineru.net/api/v4`，默认模型是 `vlm`。
- 03 页面仍然是解析预览，不会替代 01 文档导入；如果未来要让 MinerU 直接进入主 RAG 链路，应把 MinerU 解析结果持久化为 01 导入文档。

## 5. 目前最该补的材料

1. **固定测试样例表**：例如“手机充电”问题，列出 LLM-only 排序、RAG 排序、期望排序和证据命中情况。
2. **索引方式对比表**：Chroma HNSW、FAISS Flat、FAISS IVF、FAISS LSH 的速度、召回、适用场景。
3. **解析方式对比表**：本地解析和 MinerU 解析对同一复杂文档的输出差异。
4. **优化前后 prompt 对比**：展示课程 QA 任务定义如何从普通问答变成候选答案排序。
5. **论文 RAG 示例问题**：准备 2-3 个论文问题，每个问题都能展示证据、分数和最终回答。
